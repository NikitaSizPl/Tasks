# 📌 Tasks  
Сервис для управления задачами с использованием Python.  
Проект включает модули для авторизации, работы с базой данных, тестирования и контейнеризации через Docker.

---

## 🚀 Первый запуск (локально)

```bash
# Создать виртуальное окружение
python3 -m venv .venv

# Активировать окружение
source .venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
